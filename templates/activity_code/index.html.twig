{% extends 'page.html.twig' %}

{% block title %}Activity Code List{% endblock %}

{% block body %}
<section class="section">
    <div class="pagetitle">
        <h1>Manage Activity Code</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/home">Home</a></li>
                <li class="breadcrumb-item">Pages</li>
                <li class="breadcrumb-item active">Activity Code</li>
            </ol>
        </nav>
    </div><!-- End Page Title -->

    <section class="section">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center">
                            <h5 class="card-title">Activity Code List</h5>
                            <a href="{{ path('app_activity_code_new') }}" class="btn btn-primary">
                                <i class="bi bi-plus-circle"></i> New Activity Code
                            </a>
                        </div>
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th style="width: 52%;">Activity Code</th>
                                    <th style="width: 12%;">Created At</th>
                                    <th style="width: 12%;">Updated At</th>
                                    <th style="width: 12%;">Modified By</th>
                                    <th style="width: 12%;">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                            {% if activity_codes is not empty %}
                                {% for activity_code in activity_codes %}
                                <tr>
                                    <td>{{ activity_code.activityCode }}</td>
                                    <td>{{ activity_code.creationTime }}</td>
                                    <td>{{ activity_code.relativeTime }}</td>
                                    <td>
                                        {% if activity_code.modifiedBy %}
                                            {{ activity_code.modifiedBy.name }}
                                        {% else %}
                                            Unknown
                                        {% endif %}
                                    </td>
                                    <td class="text-left">
                                        <a href="{{ path('app_activity_code_show', {'id': activity_code.id}) }}" class="btn btn-secondary btn-sm my-1">
                                            <i class="bi bi-file-earmark-person"></i> Show
                                        </a>
                                        <a href="{{ path('app_activity_code_edit', {'id': activity_code.id}) }}" class="btn btn-warning btn-sm my-1">
                                            <i class="bi bi-pencil"></i> Edit
                                        </a>
                                    </td>
                                </tr>
                                {% endfor %}
                            {% else %}
                                <tr>
                                    <td colspan="5">No records found</td>
                                </tr>
                            {% endif %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>
</section>
{% endblock %}

{# {% extends 'base.html.twig' %}

{% block title %}ActivityCode index{% endblock %}

{% block body %}
    <h1>ActivityCode index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>ActivityCode</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for activity_code in activity_codes %}
            <tr>
                <td>{{ activity_code.id }}</td>
                <td>{{ activity_code.activityCode }}</td>
                <td>{{ activity_code.createdAt ? activity_code.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ activity_code.updatedAt ? activity_code.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_activity_code_show', {'id': activity_code.id}) }}">show</a>
                    <a href="{{ path('app_activity_code_edit', {'id': activity_code.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_activity_code_new') }}">Create new</a>
{% endblock %} #}
