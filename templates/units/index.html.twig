{% extends 'page.html.twig' %}

{% block title %}Units List{% endblock %}

{% block body %}
<section class="section">
    <div class="pagetitle">
        <h1>Manage List</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/home">Home</a></li>
                <li class="breadcrumb-item">Pages</li>
                <li class="breadcrumb-item active">Units</li>
            </ol>
        </nav>
    </div><!-- End Page Title -->

    <section class="section">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center">
                            <h5 class="card-title">Units List</h5>
                            <a href="{{ path('app_units_new') }}" class="btn btn-primary">
                                <i class="bi bi-plus-circle"></i> Create New Unit
                            </a>
                        </div>
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th style="width: 52%;">Unit Name</th>
                                    <th style="width: 12%;">Created At</th>
                                    <th style="width: 12%;">Updated At</th>
                                    <th style="width: 12%;">Modified By</th>
                                    <th style="width: 12%;">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                            {% if units is not empty %}
                                {% for unit in units %}
                                <tr>
                                    <td>{{ unit.name }}</td>
                                    {# <td>{{ unit.updatedAt|date('Y-m-d H:i') }}</td> #}
                                    <td>{{ unit.creationTime }}</td>
                                    <td>{{ unit.relativeTime }}</td>
                                    <td>
                                        {% if unit.modifiedBy %}
                                            {{ unit.modifiedBy.name }}
                                        {% else %}
                                            Unknown
                                        {% endif %}
                                    </td>
                                    <td class="text-left">
                                        <a href="{{ path('app_units_show', {'id': unit.id}) }}" class="btn btn-secondary btn-sm my-1">
                                            <i class="bi bi-file-earmark-person"></i> Show
                                        </a>
                                        <a href="{{ path('app_units_edit', {'id': unit.id}) }}" class="btn btn-warning btn-sm my-1">
                                            <i class="bi bi-pencil"></i> Edit
                                        </a>
                                    </td>
                                </tr>
                                {% endfor %}
                            {% else %}
                                <tr>
                                    <td colspan="5">No records found</td>
                                </tr>
                            {% endif %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>
</section>
{% endblock %}

{# {% extends 'base.html.twig' %}

{% block title %}Units index{% endblock %}

{% block body %}
    <h1>Units index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>Modified By</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for unit in units %}
            <tr>
                <td>{{ unit.id }}</td>
                <td>{{ unit.name }}</td>
                <td>{{ unit.createdAt ? unit.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ unit.updatedAt ? unit.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    {% if unit.modifiedBy %}
                        {{ unit.modifiedBy.name }}
                    {% else %}
                        Unknown
                    {% endif %}
                </td>
                <td>
                    <a href="{{ path('app_units_show', {'id': unit.id}) }}">show</a>
                    <a href="{{ path('app_units_edit', {'id': unit.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_units_new') }}">Create new</a>
{% endblock %} #}
